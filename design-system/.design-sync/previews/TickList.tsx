import * as React from 'react';
import { TickList } from '@nellie/design-system';

export const Default = () => (
  <TickList
    items={[
      'The nellie tablet in its protective case',
      'nellie connect app for the whole family',
      'Free tracked UK delivery',
      'Friendly setup, done for you',
    ]}
  />
);
